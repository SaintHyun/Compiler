int x;

int f(int x, bool b) { 
  if (b) { 
    return x + 1;
  } else {
    return x - 1;
  }
}

int g(int y) {
  int n;
  n = 10;
  n = f(n / 5, false);
  if (n || false) {
    n = f(n, (true && false) == true);
  }
  n = f(n + 2);
  return 1;
}
